{
  "name": "soldier-signout-system",
  "version": "1.0.0",
  "description": "A modern web application for tracking military soldier sign-outs and returns",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:ssl": "ENABLE_HTTPS=true SSL_CRT_FILE=./cert.crt SSL_KEY_FILE=./cert.key node server.js",
    "dev": "nodemon server.js",
    "dev:ssl": "ENABLE_HTTPS=true SSL_CRT_FILE=./cert.crt SSL_KEY_FILE=./cert.key nodemon server.js",
    "setup": "node setup.js",
    "generate-certs": "openssl req -x509 -newkey rsa:4096 -keyout cert.key -out cert.crt -days 365 -nodes -subj '/CN=localhost'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "military",
    "signout",
    "tracking",
    "nodejs",
    "express",
    "sqlite"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "express-session": "^1.18.1",
    "express-validator": "^7.2.1",
    "helmet": "^8.1.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
